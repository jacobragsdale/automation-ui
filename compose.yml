services:
  automation-ui:
    image: ${IMAGE_REF:-automation-ui:latest}
    platform: ${IMAGE_PLATFORM:-linux/amd64}
    container_name: automation-ui
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8080}:80"
